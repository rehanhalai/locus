{
  "name": "locus",
  "version": "0.1.0",
  "private": true,
  "description": "Multi-Vendor DVR/NVR Forensic Analysis Tool for Standardized Acquisition, Recovery, and Analysis of Surveillance Evidence.",
  "scripts": {
    "dev:backend": "cd backend && uv run uvicorn app.main:app --reload --port 8000",
    "dev:frontend": "pnpm --filter frontend dev",
    "dev": "concurrently -k -n \"BACKEND,FRONTEND\" -c \"blue,green\" \"pnpm run dev:backend\" \"pnpm run dev:frontend\"",
    "setup:backend": "cd backend && uv sync",
    "setup": "pnpm install && pnpm run setup:backend",
    "lint:backend": "cd backend && uv run ruff check .",
    "lint": "pnpm --filter frontend lint && pnpm run lint:backend",
    "format:backend": "cd backend && uv run ruff format .",
    "format": "pnpm --filter frontend format && pnpm run format:backend",
    "db:reset": "cd backend && uv run python scripts/reset_environment.py"
  },
  "devDependencies": {
    "concurrently": "^9.1.2",
    "cross-env": "^7.0.3"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/rehanhalai/locus.git"
  },
  "author": "Rehan Halai",
  "license": "ISC"
}
